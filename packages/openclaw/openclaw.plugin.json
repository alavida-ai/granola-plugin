{
  "id": "granola",
  "name": "granola",
  "description": "Read meeting notes, AI summaries, transcripts, and folders from Granola.ai. Read-only — Granola's public API has no write endpoints.",
  "skills": ["./skills"],
  "contracts": {
    "tools": ["granola_list_notes", "granola_read_note", "granola_list_folders"]
  },
  "activation": { "onStartup": true },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "granolaApiKey": {
        "type": "string",
        "description": "Granola API key (grn_...). If unset, falls back to GRANOLA_API_KEY env var."
      }
    }
  }
}
